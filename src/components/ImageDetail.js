import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ImageDetail = () => {
  return <Text>Show images of forest</Text>;
};

const styles = StyleSheet.create({});

export default ImageDetail;
